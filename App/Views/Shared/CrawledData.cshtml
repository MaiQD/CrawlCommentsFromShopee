@model IList<Rating>
@{
	Layout = null;
	
	int i = 0;
}
@if (Model != null && Model.Count > 0)
{
	<label>Found @Model.Count comments</label>
	<table class="table">
		<thead class="thead-dark">
			<tr>
				<th scope="col">Stt</th>
				<th scope="col">Rating star</th>
				<th scope="col">Comment</th>
			</tr>
		</thead>
		<tbody>

			@foreach (var item in Model)
			{
				<tr>
					<th scope="row">@(++i)</th>
					<td class="row">
						@await Html.PartialAsync("_RatingStar.cshtml", item.rating_star)

					</td>
					<td>@item.comment</td>
				</tr>
			}


		</tbody>
	</table>
}
else
{
	<text>No comments</text>

}
<style>
	.checked {
		color: orange;
	}
</style>